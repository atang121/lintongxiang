# 2026-04-30

## 上线前审计与修复
- 修复 admin.ts 缺少 getOne 导入（高严重度，交换管理功能完全无法使用）
- 修复 6 个文件中品牌名"邻里童享"残留 → "童邻市集"（index.ts、mail.ts 4处、feishuWebhook.ts、seeds.ts、.env）
- 修复 rateLimit.ts ipKeyGenerator 参数类型错误（应传 req.ip 而非 req 对象）
- 修复后前后端编译均零错误
- 发现但暂未修复：sms.ts 凭证映射错误（APP_ID 被混淆为 SecretId）、会话未读数硬编码为 0、图片上传无大小限制
- 输出完整审计报告：审计报告_上线前.md

## 上线前安全加固（第二轮）
- **exchanges.ts**：禁止自己和自己交换(BUG-03)、所有交换操作检查用户状态(BUG-01/02)、物品pending时拒绝新预约(BUG-20)、交换完成通知双方(BUG-21)、GET路由返回全部记录(BUG-43)
- **users.ts**：POST/PUT 增加认证和所有权检查(BUG-10/11)
- **messages.ts**：消息已读增加收件人权限验证(BUG-07)、conversations unread查询真实未读数(BUG-42)
- **notifications.ts**：通知已读增加所属用户验证(BUG-08)
- **wechatAuth.ts**：JWT_SECRET未设置时生产环境终止(BUG-34)、token包含isAdmin字段(BUG-04)
- **auth.ts**：email-login增加用户状态检查(BUG-05)
- **app.ts**：添加全局错误处理中间件(BUG-32)
- **rateLimit.ts**：新增uploadLimiter/feedbackLimiter/publishLimiter(BUG-39/40/41)
- **uploads.ts**：添加base64大小限制10MB(BUG-22)、应用uploadLimiter
- **storage.ts**：MIME类型白名单jpeg/png/gif/webp(BUG-23)
- **mail.ts**：反馈HTML模板转义content(BUG-18)
- **admin.ts**：用户搜索LIKE通配符转义(BUG-25)
- **.env.example**：真实邮箱替换为占位符(BUG-30)、补充飞书+COS配置(BUG-31)
- **feedback.ts/items.ts**：应用专项限流中间件
- 修复后 tsc --noEmit 零错误，后端服务正常启动
