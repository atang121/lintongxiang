# 2026-05-01 交接记录

## 已完成

- 上线前安全加固：短信 preview 生产保护、上传路径/扩展名白名单、生产空库不自动灌 demo、预约接口鉴权、公开用户详情脱敏、管理路由顺序、恢复用户时恢复物品。
- 首次注册：昵称必填，孩子年龄段和孩子数量选填，可跳过。
- 发布需求：无图时使用 `WantedCover`，不再显示空白图片；发布闲置仍要求上传图片。
- 详情图片：`ItemImageCarousel` 支持多图滑动、缩略图、计数、横竖图统一展示。
- 邻里等级：用完成交换次数显示 `新邻居`、`有过交换`、`靠谱邻居`、`热心邻居`，替代小数信用评分。
- 预约流程：发布者确认交接，预约者可取消、联系、提醒确认；管理员详情页保留强制状态操作。
- 反馈投递：飞书机器人、飞书多维表格、邮件兜底均有投递诊断字段。
- 本机运维：新增 PM2/构建/备份/健康检查面板和 SQLite 备份脚本。
- 详情页交互：移动端详情页隐藏全局底部导航，收藏和分享有明确反馈；分享支持微信右上角指引和复制链接。
- PWA：新增 `manifest.webmanifest`、`/app-icon.svg` 和首页“添加到桌面”提示。
- 私信：消息页可打开会话并回复，打开会话时标记来信已读；私信不再重复进入通知列表。

## 当前验证结果

- `cd h5 && node --test test/*.mjs`：通过，15/15。
- `cd h5 && npm exec tsc -- --noEmit`：通过。
- `cd h5 && npm run build`：通过。
- `cd server && npm run build`：通过。

## 上线前仍要确认

- 云服务器或本机 PM2 是否使用最新构建并已重启。
- `server/.env` 不要使用开发弱密钥；`JWT_SECRET`、`FRONTEND_URL`、`SERVER_PUBLIC_URL`、飞书、SMTP、短信按现网填写。
- SQLite 至少配置每日备份，上线前手动执行一次 `cd server && npm run backup:sqlite`。
- 真实腾讯云短信上线前需实测当前 `TENCENT_SMS_APP_ID` / `TENCENT_SMS_APP_KEY` 配置是否与账号凭证匹配。
- 历史数据库中旧的 message 类型通知不会再展示，但数据仍可能存在；需要清库时再由管理员脚本处理。

## 后续阶段

2026-05-05 已完成第二轮试运营前整理，详见 `docs/handoff-2026-05-05.md`。该阶段补充了用户服务协议、协议管理工具、候补预约、私信内预约、反馈处理回复、敏感词/活物禁发、管理员删除原因通知和后台通知运营等内容。
